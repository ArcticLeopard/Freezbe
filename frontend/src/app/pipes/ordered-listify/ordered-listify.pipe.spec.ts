import { OrderedListifyPipe } from './ordered-listify.pipe';

describe('OrderedListifyPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderedListifyPipe();
    expect(pipe).toBeTruthy();
  });
});
